from django.apps import AppConfig

class SelectteamsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'selectteams'   # <--- IT SHOULD BE THE APP NAME, not your project name!
